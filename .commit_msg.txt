round17: remove all flake8 per-file exemptions, achieve strict lint zero

- Removed `per-file-ignores` from `.flake8` entirely
- Fixed `apply_mypy_ignores.py` E741: renamed loop variable `l` to `line`
- Fixed `extensions/hardware_remediation/raid_storcli.py` E501 by splitting
  the long `script_content` string over multiple lines
- Fixed `main.py`:
  - Removed 46 unused local variable assignments in startup/shutdown
    lifespan blocks (kept `_safe_init` calls for side effects)
  - Removed 2 duplicate imports causing F811
    (`L4_STORAGE_CONFIG`, `get_l4_storage_manager`)

Verified with a strict `flake8` config (no per-file exemptions):
- `python -m flake8 .` ✅ 0 errors
- `python -m compileall -q` ✅
- `python -m pytest -q` ✅ 31 passed

Generated with Devin (https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
