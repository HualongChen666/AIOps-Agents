name: P1 Enhanced CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    - cron: '0 2 * * *'

env:
  COVERAGE_THRESHOLD: 80
  PYTHON_VERSION: '3.12'

jobs:
