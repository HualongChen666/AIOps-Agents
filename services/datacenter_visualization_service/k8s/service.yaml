apiVersion: v1
kind: Service
metadata:
  name: datacenter-visualization
  labels:
    app: datacenter-visualization
spec:
  selector:
    app: datacenter-visualization
  ports:
    - name: http
      port: 9545
      targetPort: 9545
  type: ClusterIP
