# Workflow Engine Service

A FastAPI microservice for Workflow Engine operations.

## Run

```bash
uvicorn services.workflow_engine_service.main_app:app --host 0.0.0.0 --port 9524
```

## Docker Compose

```bash
cd services/workflow_engine_service
docker-compose up -d
```
