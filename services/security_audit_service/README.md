# Security Audit Service

A FastAPI microservice for Security Audit operations.

## Run

```bash
uvicorn services.security_audit_service.main_app:app --host 0.0.0.0 --port 9551
```

## Docker Compose

```bash
cd services/security_audit_service
docker-compose up -d
```
