# Service Mesh Service

A FastAPI microservice for Service Mesh operations.

## Run

```bash
uvicorn services.service_mesh_service.main_app:app --host 0.0.0.0 --port 9520
```

## Docker Compose

```bash
cd services/service_mesh_service
docker-compose up -d
```
