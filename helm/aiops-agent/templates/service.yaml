apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiops-agent.fullname" . }}
  labels:
    {{- include "aiops-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.aiops.prometheus.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "aiops-agent.selectorLabels" . | nindent 4 }}
