apiVersion: v1
kind: Service
metadata:
  name: redis-shard
  labels:
    app: redis-shard
spec:
  selector:
    app: redis-shard
  ports:
    - name: http
      port: 9502
      targetPort: 9502
  type: ClusterIP
