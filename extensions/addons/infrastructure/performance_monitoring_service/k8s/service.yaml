apiVersion: v1
kind: Service
metadata:
  name: performance-monitoring
  labels:
    app: performance-monitoring
spec:
  selector:
    app: performance-monitoring
  ports:
    - name: http
      port: 9560
      targetPort: 9560
  type: ClusterIP
