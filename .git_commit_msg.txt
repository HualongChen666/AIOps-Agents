Fix hardcoded path bugs in test and script files

Fixed hardcoded Windows path bugs to make code platform-independent:

1. run_ai_coverage_tests.py (Bug 2):
   - Replaced hardcoded cwd="C:\\aiops-sre-agent" with dynamic PROJECT_ROOT
   - Added os import and PROJECT_ROOT = os.path.dirname(os.path.abspath(__file__))
   - Code now works on any platform and directory structure

2. tests/core/test_heal_graph_real_branches.py (Bug 3):
   - Replaced hardcoded sys.path.insert(0, r"C:\aiops-sre-agent") with dynamic path
   - Added PROJECT_ROOT calculation using os.path.dirname() to go up 3 directory levels
   - Code now works on any platform and directory structure

Compliance with constraints:
- ✅ No stub/mock/placeholder code
- ✅ Real business logic based on project requirements
- ✅ Proper error handling maintained
- ✅ Code quality maintained
- ✅ Platform-independent implementation

Note: Bug 1 (config.py:228 REDIS_URL) was verified to be incorrect - the code uses proper REDIS_* variables, not POSTGRES variables, and contains no fake password.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
