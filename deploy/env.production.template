# AIOps Agent Production Environment Configuration Template
# 生产环境配置模板
#
# 使用方法：
# 1. 复制此文件为 .env
# 2. 修改需要自定义的配置值
# 3. 运行应用前加载此配置

# =============================
# ✅ 已配置的真实值（LANGFUSE + MiniMax）
# =============================

# LANGFUSE API 配置
LANGFUSE_SECRET_KEY=sk-lf-2b8d1775-e9e9-4540-96ee-cd39aa1a8535
LANGFUSE_PUBLIC_KEY=pk-lf-71c029a1-a4ac-4c59-820c-509356d8fe24
LANGFUSE_BASE_URL=https://cloud.langfuse.com
LANGFUSE_HOST=https://cloud.langfuse.com
LANGFUSE_SESSION_ID=

# MiniMax API 配置
AI_ENABLED=true
AI_API_KEY=sk-api-CiRdR9-EjT9acHwx4PMUTu2PUniNENzp6RwIUiBOa9uFkHHJy0hj7m52MLQn-9N2HXroieq0dNs_2YXxvxDWPRXMnkoTpJU2N_d93dSpnONjcIzE9_IbxE8
AI_BASE_URL=https://api.minimaxi.com/v1
AI_MODEL=MiniMax-Text-01

# OpenAI API 配置（如果需要）
OPENAI_API_KEY=

# =============================
# 🔴 必须修改的配置（生产环境必需）
# =============================

# 环境标识
ENVIRONMENT=production

# PostgreSQL 数据库配置
POSTGRES_HOST=your-db-host.example.com
POSTGRES_PORT=5432
POSTGRES_USER=your-db-user
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
POSTGRES_DB=aiops_prod
POSTGRES_DATABASE=aiops_prod

# Redis 配置
REDIS_HOST=your-redis-host.example.com
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
REDIS_MODE=standalone

# JWT 安全配置
JWT_SECRET_KEY=75ODxkOYGWDinKe_TOegUwrd4bNH04HPJBgRIxpfleA
JWT_ALGORITHM=HS256
JWT_ACCESS_EXPIRE_MINUTES=30
JWT_ISSUER=aiops-agent
JWT_AUDIENCE=aiops-api
BCRYPT_ROUNDS=12

# 内部API密钥
INTERNAL_API_KEY=GLl8LMzKfhE7Y2fYnk63sBuq_ATkXsaadRMTpoba-g0

# =============================
# 🟡 推荐配置的配置（生产环境推荐）
# =============================

# CORS 配置
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOW_HEADERS=*

# HTTPS/TLS 配置
HTTPS_ENABLED=true
SSL_CERT_FILE=/etc/ssl/certs/yourdomain.crt
SSL_KEY_FILE=/etc/ssl/private/yourdomain.key

# 监控和日志配置
OTEL_COLLECTOR_ENDPOINT=http://otel-collector:4318
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
LOKI_HOST=loki.example.com
LOKI_PORT=3100
TEMPO_HOST=tempo.example.com
VICTORIAMETRICS_HOST=victoriametrics.example.com

LOG_LEVEL=INFO
LOG_DIR=/var/log/aiops

# Qdrant 向量数据库配置
QDRANT_HOST=qdrant.example.com
QDRANT_PORT=6333

# =============================
# 🟢 可选配置（根据实际需求）
# =============================

# 通知集成
TEAMS_WEBHOOK_URL=
TEAMS_DEFAULT_CHANNEL=General
TEAMS_WEBHOOK=
TEAMS_CHANNEL=aiops-alerts

SLACK_BOT_TOKEN=
SLACK_CHANNEL=#aiops-alerts

# ITSM 集成
SERVICENOW_INSTANCE=
SERVICENOW_USERNAME=
SERVICENOW_PASSWORD=

JIRA_URL=
JIRA_USERNAME=
JIRA_API_TOKEN=

# 其他存储
ELASTICSEARCH_HOST=elasticsearch.example.com
ELASTICSEARCH_PORT=9200
KAFKA_BROKERS=kafka1.example.com:9092,kafka2.example.com:9092
DATAHUB_HOST=datahub.example.com
DATAHUB_PORT=8080

# RAG 配置
RAG_ENABLED=true
RAG_COLLECTION_NAME=aiops_knowledge
RAG_EMBEDDING_MODEL=all-MiniLM-L6-v2
RAG_SCORE_THRESHOLD=0.5

# 历史配置
HISTORY_MAX_POINTS=60

# 工作流配置
WF_NODE_MIN_DELAY_MS=500
WF_NODE_MAX_DELAY_MS=1200

# 备份配置
BACKUP_LOCATION=/backups
BACKUP_TYPES=database,config,logs

# 缓存配置
CACHE_BACKEND=redis
CACHE_KEY_PREFIX=aiops_prod
CACHE_SERIALIZATION_FORMAT=json