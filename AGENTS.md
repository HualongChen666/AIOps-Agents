# AIOps SRE Agent 项目执行约束

必须严格遵守以下约束条件：

1. 测试框架约束
并行测试：所有测试任务必须使用pytest-xdist进行并行测试，提升测试效率
证据要求：测试配置文件中必须包含pytest-xdist配置，并提供配置文件路径作为证据
2. 性能控制约束
速率限制规避：所有API调用和数据库操作必须考虑速率限制，避免触发限流
性能控制：批量操作必须分批处理，避免系统过载
证据要求：代码中必须包含速率限制和分批处理的实现，并提供代码证据
3. 业务逻辑真实性约束
真实业务逻辑：所有修复必须符合项目实际情况，使用真实的业务逻辑
支持能力：修复的功能必须具备对应的support能力（日志、监控、错误处理）
可运行代码：所有代码必须是真正可运行的，不能是伪代码或注释代码
证据要求：每个修复必须提供可运行的代码证据，包括函数调用链和执行路径
4. 客观性约束
不得主观发散：所有决策必须基于代码证据和数据，不得主观臆想
不得臆想延伸：不得添加计划中未明确要求的功能
不得随意收敛：不得随意简化或删除计划中明确要求的功能
证据要求：所有决策必须提供代码证据链支持
5. 代码质量约束
禁止stub/骨架/mock/占位符：所有代码必须是完整实现，不得使用stub、骨架、mock或占位符
禁止硬编码：所有配置必须使用环境变量或配置文件，不得硬编码，不得出现lint问题
证据要求：代码审查必须确认无stub/骨架/mock/占位符/硬编码
6. 证据链要求
完整证据链：每个任务必须提供完整的证据链，包括：
当前状态证据（代码行号、文件路径）
修改后的代码证据
测试运行证据
功能验证证据
证据格式：证据必须包含文件路径、行号、代码片段
7. 交付约束
GitHub main分支：所有完成后必须推送到GitHub的main分支
代码审查：推送前必须通过代码审查
CI/CD验证：推送前必须通过CI/CD验证
证据要求：提供GitHub推送记录和CI/CD运行记录
8. 数据迁移约束
零数据丢失：数据迁移必须保证零数据丢失
数据一致性：迁移前后数据必须保持一致
可回滚：必须提供完整的回滚方案和回滚脚本
证据要求：提供数据一致性校验脚本和校验结果
9. 安全约束
授权检查：所有端点必须包含授权检查
安全头：必须配置完整的安全头
密钥管理：必须使用安全的密钥管理方案
证据要求：提供安全测试报告和代码审查记录
10. 性能约束
性能基线：必须建立性能基线，优化后必须达到目标指标
监控验证：必须提供性能监控数据和验证报告
证据要求：提供性能测试报告和监控仪表板截图

## 执行指南 - 减少幻觉和主观性

### 核心约束策略

#### 1. 任务描述层面

- **明确边界**：明确指定要做什么和不要做什么
- **提供证据要求**：要求每个决策必须提供代码证据链
- **禁止扩展**：明确禁止添加计划中未明确要求的功能
- **禁止简化**：明确禁止随意简化或删除计划中明确要求的功能

#### 2. 执行流程层面

- **先探索后执行**：要求先使用只读工具（grep、read）探索代码库，理解现有模式
- **证据先行**：要求在修改前先提供当前状态证据（文件路径、行号、代码片段）
- **分步验证**：要求每个步骤完成后提供验证证据

#### 3. 代码质量层面

- **禁止占位符**：明确禁止使用 stub、骨架、mock 或占位符
- **禁止硬编码**：要求所有配置使用环境变量或配置文件
- **真实业务逻辑**：要求所有修复必须符合项目实际情况

#### 4. 验证层面

- **可运行性要求**：要求所有代码必须是真正可运行的
- **测试覆盖**：要求提供测试运行证据
- **功能验证**：要求提供功能验证证据

### 具体实施模板

#### 对于 Coding 任务

```
请执行以下任务：
1. 先使用 grep/read 工具探索相关代码，提供当前状态证据
2. 基于现有代码模式进行修改，不得主观臆想
3. 每个修改必须提供：
   - 修改前的代码证据（文件路径、行号）
   - 修改后的代码证据
   - 测试运行证据
4. 禁止添加计划中未明确要求的功能
5. 禁止使用 stub/骨架/mock/占位符
6. 所有配置必须使用环境变量或配置文件
```

#### 对于前端 UI/UX 设计

```
请执行以下任务：
1. 先探索现有前端代码模式，提供当前状态证据
2. 基于现有设计系统（如 DDS）进行设计，不得主观臆想
3. 每个设计决策必须提供：
   - 现有代码证据
   - 设计规范依据
   - 实现代码证据
4. 禁止添加计划中未明确要求的UI元素
5. 禁止随意简化或删除计划中明确要求的功能
```

### 关键约束短语

- "不得主观发散"
- "不得臆想延伸"  
- "不得随意收敛"
- "必须提供代码证据链"
- "禁止 stub/骨架/mock/占位符"
- "禁止硬编码"

### 验证机制

每次任务完成后，要求提供：

1. **修改证据**：文件路径、行号、代码片段
2. **测试证据**：测试运行结果
3. **功能证据**：功能验证截图或日志

### 证据链格式要求

#### 当前状态证据

- 文件路径：`/path/to/file.py`
- 行号范围：`10-20`
- 代码片段：具体的代码内容

#### 修改后证据

- 文件路径：`/path/to/file.py`
- 行号范围：`10-20`
- 修改后代码片段：修改后的具体代码内容

#### 测试证据

- 测试命令：`pytest -xvs tests/`
- 测试结果：具体的测试输出
- 覆盖率报告：覆盖率数据

#### 功能验证证据

- 验证方法：手动测试或自动化测试
- 验证结果：具体的验证输出
- 截图或日志：相关的截图或日志文件

### 违规检查清单

在执行任务前，检查是否：

- [ ] 是否基于现有代码模式进行修改？
- [ ] 是否提供了当前状态证据？
- [ ] 是否禁止了主观发散？
- [ ] 是否禁止了臆想延伸？
- [ ] 是否禁止了随意收敛？
- [ ] 是否使用了 stub/骨架/mock/占位符？
- [ ] 是否存在硬编码？
- [ ] 是否提供了完整的证据链？
- [ ] 代码是否真正可运行？
- [ ] 是否提供了测试验证证据？

### 紧急停止条件

如果出现以下情况，立即停止并询问用户：

- 发现需要主观判断的决策点
- 现有代码模式不清晰
- 缺少必要的配置或依赖
- 任务描述存在歧义
- 可能影响数据安全或系统稳定性
